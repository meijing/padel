<%= form_for(@team) do |f| %>
  <% if @team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team.errors.count, "error") %> prohibited this team from being saved:</h2>

      <ul>
      <% @team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="team_panel">
    <div class="field">
      <%= f.label :player1_id %><br />
      <%= collection_select :team, :player1_id, Player.all, :id, :name %><br />
    </div>
    <div class="field">
      <%= f.label :player2_id %><br />
      <%= collection_select :team, :player2_id, Player.all, :id, :name %><br />
    </div>
    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
<% end %>
